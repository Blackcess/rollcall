// import cookie from "cookie"


// export function createManualSessionDeserializer(pool){
//     return async function manualSessionDeserializer(req,res,next){

//         try {
//             //try to get session cookie from request
//             let sid = null;
//             //try to get it from the cookies section in the request
//             if (request.headers.cookie){
//                 const cookies = cookie.parse(req.headers.cookie);
//             }
//         } catch (error) {
            
//         }
//     }

// }